global proc shelf_ZOMBIE_TOOLS () {
    global string $gBuffStr;
    global string $gBuffStr0;
    global string $gBuffStr1;


    shelfButton
        -enableCommandRepeat 1
        -enable 1
        -width 35
        -height 35
        -manage 1
        -visible 1
        -preventOverride 0
        -annotation "SCREENSHOT" 
        -enableBackground 0
        -backgroundColor 0 0 0 
        -highlightColor 0.321569 0.521569 0.65098 
        -align "center" 
        -label "SCREENSHOT" 
        -labelOffset 0
        -rotation 0
        -flipX 0
        -flipY 0
        -useAlpha 1
        -font "plainLabelFont" 
        -overlayLabelColor 0.8 0.8 0.8 
        -overlayLabelBackColor 0 0 0 0.5 
        -image "snapshot.png" 
        -image1 "snapshot.png" 
        -style "iconOnly" 
        -marginWidth 1
        -marginHeight 1
        -command "import maya.OpenMayaUI as OpenMayaUI\nimport maya.OpenMaya as OpenMaya\nimport random\nimport os\n\npath = cmds.workspace(q=True, rootDirectory=True)\npath = \"{}screenshots/\".format(path)\n\nif not os.path.exists(path):\n\tos.mkdir(path)\n\nview = OpenMayaUI.M3dView.active3dView()\nimg = OpenMaya.MImage()\nview.readColorBuffer(img, 1)\nimg.writeToFile(\"{}{}.jpg\".format(path, int(random.random() * 10000)), \"jpg\")" 
        -sourceType "python" 
        -commandRepeatable 1
        -flat 1
    ;
    shelfButton
        -enableCommandRepeat 1
        -enable 1
        -width 35
        -height 35
        -manage 1
        -visible 1
        -preventOverride 0
        -annotation "CLEANUP FAST RUN MAYA\n" 
        -enableBackground 0
        -backgroundColor 0 0 0 
        -highlightColor 0.321569 0.521569 0.65098 
        -align "center" 
        -label "#CLEANUP FAST RUN MAYA\n" 
        -labelOffset 0
        -rotation 0
        -flipX 0
        -flipY 0
        -useAlpha 1
        -font "plainLabelFont" 
        -overlayLabelColor 0.8 0.8 0.8 
        -overlayLabelBackColor 0 0 0 0.5 
        -image "C:/Users/ZOMBIEZILLA/AppData/Roaming/ZombieEnv/2018/prefs/icons/fast.png" 
        -image1 "C:/Users/ZOMBIEZILLA/AppData/Roaming/ZombieEnv/2018/prefs/icons/fast.png" 
        -style "iconOnly" 
        -marginWidth 1
        -marginHeight 1
        -command "#CLEANUP FAST RUN MAYA\nimport maya.cmds as cmds\nimport maya.mel as mel\nimport os\nimport glob\nimport shutil\nimport time\n\nimport maya.cmds as cmds\n\nwindow = cmds.window(title=\"CLEANUP MAYA\",maximizeButton=False, minimizeButton=False, sizeable=False, height=60)\ncmds.columnLayout( columnAttach=('both', 25), rowSpacing=8, adjustableColumn=True )\ncmds.text(\"\")\nprogressControl = cmds.progressBar(width=250, maxValue=15)\n\ncmds.showWindow( window )\n\ntotal = 0\npath = os.environ[\"APPDATA\"]\npath = \"{}\\\\zombieEnv\".format(path)\nshelf_path = \"{}\\\\2018\\prefs\\shelves\\\\\".format(path)\nfiles = os.listdir(shelf_path)\nshelfs = []\nfor s in files:\n    shelf = s.replace(\"shelf_\",\"\").replace(\".mel\",\"\")\n    shelfs.append(shelf)\n\nif os.path.exists(path):   \n    for i in files:\n        if not \"ZOMBIE\"in i:\n            try:\n                cmds.deleteUI(\"{}\".format(i.replace(\"shelf_\",\"\").replace(\".mel\", \"\")))\n                print(\"{}\".format(i.replace(\"shelf_\",\"\").replace(\".mel\", \"\")))\n                time.sleep(1)\n                total += 1\n                cmds.progressBar(progressControl, edit=True, step=total)\n            except:\n                pass\n\n\nfor p in [\"Substance\",\"bifmeshio\",\"bifrostshellnode\", \"bifrostvisplugin\"]:\n    if cmds.pluginInfo(p, query = True, loaded = True):        \n        cmds.unloadPlugin(p, force=True)\n        cmds.pluginInfo(p, edit=True, autoload=False)\n        total += 1\n        cmds.progressBar(progressControl, edit=True, step=total)\n        time.sleep(2)\n\n\ncmds.optionVar(stringValue=(\"colorManagementPolicyFileName\",\"R:/Zombie Dropbox/Zombie Studio (freelance)/INHOUSE/OCIO/maya/ACES.xml\"))\ntotal += 1\nprint(total)\ncmds.progressBar(progressControl, edit=True, step=total)\ncmds.savePrefs()\ncmds.deleteUI(window)\ncmds.quit(force=True)" 
        -sourceType "python" 
        -commandRepeatable 1
        -flat 1
    ;

} 
